import sys

class Salyangoz:
    çık = sys.exit
    boyut = sys.getsizeof
    platform = sys.platform
    argümanlar = sys.argv

    modüller = {}
    versiyon = (0, 1)
    dizinler = []

    std_giriş = sys.stdin
    std_çıkış = sys.stdout
    std_hata  = sys.stderr
